# העתיקי ל-.env והדביקי מחרוזת חיבור מ-Atlas → Connect → Drivers
# אל תעלי את .env ל-Git

PORT=5000
MONGO_URI=mongodb+srv://USER:PASSWORD@CLUSTER.0v0nx3t.mongodb.net/DATABASE_NAME

# מקור האתר הסטטי (React על Render) — CORS
# אפשר ערך אחד או כמה מופרדים בפסיקים
CLIENT_ORIGIN=https://songixclient.onrender.com
# CORS_ORIGINS=https://songixclient.onrender.com,http://localhost:5173

# אופציונלי — SMTP לשליחת קוד OTP במייל
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
